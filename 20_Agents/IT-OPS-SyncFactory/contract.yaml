schema_version: "1.0"
actor_id: IT-OPS-SyncFactory
version: "1.0.0"
input:
  optional:
    - date_depuis: string
    - manifest_version: string
output:
  artifacts:
    - type: yaml
      title: CURRENT_STATE mis à jour
      path: "00_FACTORY_SYNC/CURRENT_STATE.yaml"
    - type: yaml
      title: Rapport Factory outbox
      path: "00_FACTORY_SYNC/outbox/FACTORY_SYNC_{date}.yaml"
    - type: markdown
      title: CHANGELOG entry
      path: "00_FACTORY_SYNC/CHANGELOG.md"
